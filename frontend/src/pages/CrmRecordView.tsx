import { ArrowLeft, LockKeyhole, UserRound } from 'lucide-react'
import { customer } from '../mockData'
export function CrmRecordView({ onNavigate }: { onNavigate: (p: string) => void }) { return <div className="min-h-screen bg-slate-100 p-5 text-slate-900"><main className="mx-auto max-w-4xl"><button onClick={() => onNavigate('/call/demo-1')} className="mb-5 inline-flex items-center gap-1 text-sm font-bold text-indigo-700"><ArrowLeft size={16}/>Back to call</button><div className="panel overflow-hidden"><div className="bg-slate-900 p-6 text-white"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Read-only CRM record</p><h1 className="mt-1 text-3xl font-black">{customer.name}</h1><p className="mt-1 text-slate-300">{customer.id} · {customer.city}</p></div><div className="grid gap-6 p-6 md:grid-cols-2"><section><h2 className="flex items-center gap-2"><UserRound size={19} className="text-indigo-600"/>Customer profile</h2><dl className="mt-3 space-y-2 text-sm"><div className="flex justify-between border-b border-slate-100 py-2"><dt className="text-slate-500">Email</dt><dd>{customer.email}</dd></div><div className="flex justify-between border-b border-slate-100 py-2"><dt className="text-slate-500">Phone</dt><dd>{customer.phone}</dd></div></dl><h2 className="mt-6 flex items-center gap-2"><LockKeyhole size={19} className="text-rose-600"/>KYC fields on file</h2><p className="mt-1 text-xs text-rose-600">Sensitive data present — do not re-request verified fields.</p><ul className="mt-3 space-y-2">{customer.kycFields.map(k => <li key={k.label} className="rounded-lg bg-rose-50 p-3 text-sm"><b>{k.label}</b><span className="float-right text-slate-600">{k.value}</span></li>)}</ul></section><section><h2>Past interactions</h2><div className="mt-3 space-y-3">{customer.interactions.map(i => <article key={i.date} className="rounded-xl border border-slate-200 p-3"><div className="flex justify-between gap-2 text-sm"><b>{i.outcome}</b><span className="text-slate-500">{i.date}</span></div><p className="mt-1 text-sm text-slate-600">{i.note}</p></article>)}</div></section></div></div></main></div> }
