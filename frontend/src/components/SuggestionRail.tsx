import { Archive, FilePlus2, Sparkles } from 'lucide-react'
import type { Suggestion } from '../types'
export function SuggestionRail({ suggestions, onInsert }: { suggestions: Suggestion[]; onInsert: (s: Suggestion) => void }) { const active = suggestions.filter(s => s.status === 'active').slice(-3).reverse(); const archived = suggestions.filter(s => s.status === 'archived'); return <section className="panel flex min-h-0 flex-col"><div className="panel-head"><div><p className="eyebrow">Zone C</p><h2>Next-best action</h2></div><Sparkles className="text-violet-600" size={20}/></div><div className="min-h-0 flex-1 space-y-3 overflow-y-auto p-4">{active.length === 0 && <div className="rounded-xl border border-dashed border-slate-300 p-5 text-center text-sm text-slate-500">Detected customer intent will surface agent-ready guidance here.</div>}{active.map((s, i) => <article key={s.id} className="rounded-xl border border-violet-200 bg-violet-50/60 p-3.5 shadow-sm"><div className="mb-2 flex items-center justify-between"><span className="text-xs font-bold text-violet-700">#{i + 1} · {Math.round(s.confidence * 100)}% confidence</span><span className="rounded bg-white px-1.5 py-0.5 text-[10px] font-bold uppercase text-slate-500">{s.intent}</span></div><p className="text-sm font-medium leading-5 text-slate-800">{s.text}</p><p className="mt-2 text-xs leading-4 text-slate-500">Source: {s.source}</p><button onClick={() => onInsert(s)} className="mt-3 inline-flex w-full items-center justify-center gap-2 rounded-lg bg-white px-3 py-2 text-sm font-bold text-indigo-700 ring-1 ring-inset ring-indigo-200"><FilePlus2 size={15}/>Insert into notes</button></article>)}{archived.length > 0 && <div className="border-t border-slate-100 pt-3"><p className="mb-2 flex items-center gap-1 text-xs font-bold uppercase tracking-wider text-slate-400"><Archive size={13}/>Archived guidance</p>{archived.slice(-3).reverse().map(s => <div key={s.id} className="mb-2 rounded-lg bg-slate-50 p-2 text-xs text-slate-400 opacity-60 line-through">{s.intent}: {s.text}</div>)}</div>}</div></section> }
