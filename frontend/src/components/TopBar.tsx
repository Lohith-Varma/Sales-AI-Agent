import { BadgeCheck, Clock3, ExternalLink, ShieldAlert } from 'lucide-react'
import type { CrmRecord } from '../types'
export function TopBar({ consent, onConfirm, seconds, customer, onOpenCrm, onEnd }: { consent: boolean; onConfirm: () => void; seconds: number; customer: CrmRecord; onOpenCrm: () => void; onEnd: () => void }) { const time = `${String(Math.floor(seconds / 60)).padStart(2, '0')}:${String(seconds % 60).padStart(2, '0')}`; return <header className="sticky top-0 z-30 flex flex-wrap items-center justify-between gap-3 border-b border-slate-200 bg-white/95 px-5 py-3 backdrop-blur"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-indigo-600 font-black text-white">P3</div><div><p className="text-sm font-bold text-slate-900">Agent Assist</p><p className="text-xs text-slate-500">Live affordability call</p></div></div><div className="flex flex-wrap items-center gap-2 text-sm"><span className={`inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 font-semibold ${consent ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-800'}`}><BadgeCheck size={15}/>{consent ? 'Consent confirmed' : 'Consent pending'}</span>{!consent && <button onClick={onConfirm} className="rounded-lg bg-slate-900 px-3 py-1.5 font-semibold text-white">Confirm consent</button>}<span className="inline-flex items-center gap-1 rounded-lg border border-slate-200 px-3 py-1.5 font-mono text-slate-700"><Clock3 size={15}/>{time}</span><button onClick={onOpenCrm} className="rounded-lg px-2 py-1 text-left hover:bg-slate-100"><span className="font-semibold text-indigo-700">{customer.name}</span><span className="ml-1 text-xs text-slate-500">{customer.id} <ExternalLink className="inline" size={12}/></span></button>{customer.sensitiveDataOnFile && <span className="inline-flex items-center gap-1 rounded-full bg-rose-50 px-3 py-1.5 text-xs font-semibold text-rose-700"><ShieldAlert size={14}/>Sensitive data on file</span>}<button onClick={onEnd} className="rounded-lg border border-rose-200 px-3 py-1.5 font-semibold text-rose-700">End call</button></div></header> }
