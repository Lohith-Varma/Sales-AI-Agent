"""Shared contracts and execution helpers for specialized agents.

The concrete protocol is defined in ``agent.py``. It is not re-exported here so
package imports stay stable while concrete agent modules are loaded independently.
"""

__all__: tuple[str, ...] = ()
